/**app.wxss**/

/* 引入本地iconfont类应用到全局,不用在组件中单独引用 */
@import "/static/iconfont/iconfont.wxss";

/* 微信小程序系统自动在组件的最外层加一个page标签,作为组件的父标签，其高度相当于页面高度，需要手动设置为100%，否则组件即使设置高度100%，也撑不开手机屏幕 */
page {
  height: 100%;
}